# 🤖 AI Workflow Guide

**สำหรับ AI Agents ที่จะทำงานกับโปรเจคนี้**

---

## 📖 การใช้งาน

เมื่อผู้ใช้ (human) บอกว่า **"พัฒนาต่อ"** หรือ **"ทำงานต่อจากที่ค้างไว้"** ให้ทำตามขั้นตอนนี้:

### ขั้นตอนที่ 1: อ่าน MASTER_PLAN.md

```bash
# อ่านไฟล์ MASTER_PLAN.md เพื่อดูว่าทำถึงไหนแล้ว
cat MASTER_PLAN.md
```

ดูที่ checkbox `[ ]` และ `[x]`:
- `[x]` = ทำเสร็จแล้ว
- `[ ]` = ยังไม่ได้ทำ

### ขั้นตอนที่ 2: หา Task ถัดไปที่ยังไม่ได้ทำ

ค้นหา task แรกที่ยังเป็น `[ ]` และเริ่มทำจากตรงนั้น

**ตัวอย่าง**:
```markdown
- [x] 1.1. Task ที่ทำเสร็จแล้ว
- [x] 1.2. Task ที่ทำเสร็จแล้ว
- [ ] 1.3. Task ถัดไปที่ต้องทำ  ← เริ่มที่นี่!
- [ ] 1.4. Task ที่ยังไม่ถึง
```

### ขั้นตอนที่ 3: ทำ Task นั้น

ทำตามคำแนะนำใน task นั้นๆ อย่างละเอียด

### ขั้นตอนที่ 4: อัพเดท MASTER_PLAN.md

เมื่อทำ task เสร็จ ให้เปลี่ยน `[ ]` เป็น `[x]`:

```markdown
- [x] 1.3. Task ที่เพิ่งทำเสร็จ  ← เปลี่ยนเป็น [x]
```

### ขั้นตอนที่ 5: Commit การเปลี่ยนแปลง

```bash
git add .
git commit -m "feat: complete task 1.3 - [task description]"
git push origin main
```

### ขั้นตอนที่ 6: ทำ Task ถัดไป

กลับไปที่ขั้นตอนที่ 2 และทำต่อเรื่อยๆ จนกว่าจะ:
- ทำครบทุก task ใน phase ปัจจุบัน
- หรือผู้ใช้สั่งให้หยุด

---

## 🎯 กรณีพิเศษ

### กรณีที่ 1: Task ต้องการข้อมูลจากผู้ใช้

ถ้า task ระบุว่า **"แจ้งผู้ใช้"** หรือต้องการข้อมูลจากผู้ใช้:

1. หยุดทำ task นั้น
2. ถามผู้ใช้เพื่อขอข้อมูล
3. รอคำตอบ
4. เมื่อได้คำตอบแล้ว ทำ task ต่อ

**ตัวอย่าง**:
```markdown
- [ ] 1.1.1. แจ้งผู้ใช้: ขอให้สร้าง Supabase project
```

**การทำงาน**:
```
AI: "กรุณาสร้าง Supabase project และให้ข้อมูลต่อไปนี้:
     - Supabase URL
     - Anon Key
     - Service Role Key"

[รอผู้ใช้ตอบ]

User: "นี่ครับ URL=..., Anon Key=..., Service Role Key=..."

AI: [บันทึกข้อมูล และทำ task ต่อ]
```

### กรณีที่ 2: Task ไม่ชัดเจน

ถ้า task ไม่ชัดเจนหรือไม่เข้าใจ:

1. อ่าน documentation ที่เกี่ยวข้อง (ใน `docs/`)
2. ถ้ายังไม่เข้าใจ ให้ถามผู้ใช้
3. **อย่า** guess หรือทำผิด

### กรณีที่ 3: เจอ Error

ถ้าเจอ error ขณะทำ task:

1. อ่าน error message ให้ละเอียด
2. พยายามแก้ไขด้วยตัวเอง (ถ้าเป็น error ที่ชัดเจน)
3. ถ้าแก้ไม่ได้ ให้:
   - บันทึก error ไว้ใน comment ของ task
   - แจ้งผู้ใช้
   - รอคำแนะนำ

**ตัวอย่าง**:
```markdown
- [ ] 1.2.3. Install Dependencies
  <!-- Error: pnpm not found. Waiting for user guidance. -->
```

### กรณีที่ 4: การทำงานถูกขัดจังหวะ (Interruption)

ถ้าการทำงานถูกขัดจังหวะ (เช่น session timeout, user stop):

**เมื่อกลับมาทำงานใหม่**:
1. อ่าน `MASTER_PLAN.md` เพื่อดูว่าทำถึงไหน
2. อ่าน `git log` เพื่อดู commit ล่าสุด
3. เริ่มทำต่อจาก task แรกที่ยังเป็น `[ ]`

```bash
# ดู commit ล่าสุด
git log --oneline -5

# ดู task ที่ยังไม่ได้ทำ
grep "\[ \]" MASTER_PLAN.md | head -5
```

---

## 📋 Checklist สำหรับทุก Task

ก่อนทำ task ใดๆ ให้ตรวจสอบ:

- [ ] อ่าน task description ให้เข้าใจ
- [ ] ตรวจสอบว่ามี dependencies (tasks ก่อนหน้า) ทำเสร็จแล้วหรือยัง
- [ ] เตรียมข้อมูล/tools ที่จำเป็น
- [ ] ทำ task ตามคำแนะนำ
- [ ] ทดสอบว่าทำงานถูกต้อง
- [ ] อัพเดท `MASTER_PLAN.md` (เปลี่ยน `[ ]` เป็น `[x]`)
- [ ] Commit การเปลี่ยนแปลง

---

## 🔄 Workflow Loop

```
1. อ่าน MASTER_PLAN.md
   ↓
2. หา task ถัดไป ([ ])
   ↓
3. ทำ task นั้น
   ↓
4. เปลี่ยน [ ] เป็น [x]
   ↓
5. Commit & Push
   ↓
6. กลับไปที่ 1
```

**หยุดเมื่อ**:
- ทำครบทุก task ใน phase
- ผู้ใช้สั่งให้หยุด
- เจอปัญหาที่ต้องถามผู้ใช้

---

## 💡 Tips สำหรับ AI

### 1. การ Commit

**Good commit messages**:
```
feat(backend): add extension auth API endpoint
fix(extension): resolve CORS issue in image fetching
docs: update setup guide with Supabase instructions
```

**Bad commit messages**:
```
update
fix bug
wip
```

### 2. การจัดการ Files

**ก่อนแก้ไขไฟล์**:
```bash
# อ่านไฟล์เดิมก่อน
cat path/to/file.ts

# แก้ไข
# ...

# ตรวจสอบการเปลี่ยนแปลง
git diff path/to/file.ts
```

### 3. การทดสอบ

**หลังทำ task**:
```bash
# ทดสอบว่า build ผ่าน
pnpm build

# ทดสอบว่า lint ผ่าน
pnpm lint

# ทดสอบว่า dev server รันได้
pnpm dev
```

### 4. การจัดการ Environment Variables

**อย่า** commit ไฟล์ที่มี credentials จริง:
- `.env.local` ← อย่า commit
- `.env.example` ← commit ได้ (ไม่มีค่าจริง)

---

## 📚 เอกสารเพิ่มเติม

- `MASTER_PLAN.md` - รายการ tasks ทั้งหมด
- `docs/ARCHITECTURE.md` - สถาปัตยกรรมระบบ
- `docs/SETUP_GUIDE.md` - คู่มือการติดตั้ง
- `docs/DEPLOYMENT_GUIDE.md` - คู่มือการ deploy
- `packages/backend/README.md` - เอกสาร backend
- `packages/extension/README.md` - เอกสาร extension

---

## 🆘 ขอความช่วยเหลือ

ถ้าไม่แน่ใจในสิ่งใดๆ **ให้ถามผู้ใช้เสมอ** อย่า guess!

**ตัวอย่างคำถามที่ดี**:
- "Task 1.2.3 ต้องการ Supabase URL ครับ คุณมีหรือยังครับ?"
- "ผมเจอ error นี้ครับ: [error message] ควรแก้ไขอย่างไรครับ?"
- "Task นี้ไม่ชัดเจนครับ คุณต้องการให้ทำอย่างไรครับ?"

---

**สุดท้าย**: โปรเจคนี้ออกแบบมาให้ AI และ human ทำงานร่วมกันได้อย่างราบรื่น ถ้าทำตาม workflow นี้ การพัฒนาจะเป็นไปอย่างมีประสิทธิภาพและไม่สับสนครับ!
